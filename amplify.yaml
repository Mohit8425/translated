version: 2
appConfig:
  frontend:
    phases:
      build:
        commands:
          - echo "Building static site"
          - npm install
          - npm run build
    artifacts:
      baseDirectory: /
      files:
        - '**/*'
    cache:
      paths: []
  backend:
    phases:
      build:
        commands:
          - amplify generate backend